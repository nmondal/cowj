// get the fcm
fcm = _ds.fcm

// pass message
response = _ds.fcm.sendMulticast({ "_tokens" : ["a", "b"] , "_tile" : "Title", "_body" : "Body" })

// do something here
println(response.successCount)
println(response.failureCount)
